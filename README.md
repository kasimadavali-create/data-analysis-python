# data-analysis-python
This project is a semantic text search engine built using Natural Language Processing (NLP). It understands the meaning of text instead of relying on exact keyword matching. Sentence embeddings and FAISS are used for efficient similarity search. 
